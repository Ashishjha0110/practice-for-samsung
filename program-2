#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int swapnibble(int n)
{
  return ((n & 0x0F)<<4 | (n & 0xF0)>>4);
}

int main()
{
  int a;
  cin>>a;
  cout << swapnibble(a);
  return 0;
}